package model.object;

import model.object.abstractions.Word;

import java.awt.*;

public class PropertyWord extends Word {
    public PropertyWord(String name, Color color) {
        super(name, color);
    }
}

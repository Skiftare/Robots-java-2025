package model.object;

import model.object.abstractions.Word;

import java.awt.*;

// For words representing nouns like "PERS", "STUDENT"
public class NounWord extends Word {
    public NounWord(String name, Color color) {
        super(name, color);
    }
}
